# ハッシュ確認ツール 共通仕様

この文書は、各言語版が共通して満たす仕様を定義します。

## 目的

ベンダーから提供されたハッシュ値と、確認対象ファイルから算出したハッシュ値を比較し、ファイルが想定どおりであるか確認します。

## 対応アルゴリズム

- MD5
- SHA256

## 入力

各実装は、以下の入力を扱います。

- ハッシュ値を記載したテキストファイル
- 確認対象ファイル
- ハッシュアルゴリズム

## ハッシュファイルのルール

- ハッシュ値のみを1行で記載します。
- 前後の空白は許容します。
- 空行は無視します。
- 大文字・小文字は区別しません。
- 空行を除いて複数行ある場合はエラーとします。
- ハッシュ値の文字数がアルゴリズムと一致しない場合はエラーとします。
- 16進数以外の文字が含まれる場合はエラーとします。

## 自動検出ルール

ハッシュファイルまたは確認対象ファイルが明示されない場合、作業フォルダ内から自動検出します。

### ハッシュファイル

作業フォルダ内の `.txt` ファイルを候補とします。

| 候補数 | 動作 |
|---:|---|
| 0件 | エラー |
| 1件 | ハッシュファイルとして使用 |
| 2件以上 | エラー |

### 確認対象ファイル

作業フォルダ内の通常ファイルから、各実装のスクリプト、実行補助ファイル、ログ、ハッシュファイルを除外したものを候補とします。

共通の除外拡張子は以下です。

- `.txt`
- `.log`
- `.bat`
- `.cmd`

各言語版は、自身のソースファイルや実行補助ファイルを追加で除外できます。

| 候補数 | 動作 |
|---:|---|
| 0件 | エラー |
| 1件 | 確認対象ファイルとして使用 |
| 2件以上 | エラー |

## 終了コード

| 終了コード | 意味 |
|---:|---|
| 0 | ハッシュ値が一致 |
| 1 | ハッシュ値が不一致 |
| 2 | 入力ファイルの検出エラー |
| 3 | ファイル読み取りエラー |
| 4 | ハッシュ値の形式エラー |
| 5 | ハッシュ値算出エラー、または想定外エラー |

## 出力方針

- 成功、不一致、エラーを明確に表示します。
- 使用した作業フォルダ、ハッシュ方式、ハッシュファイル、確認対象ファイルを表示します。
- 比較したハッシュ値は小文字に正規化して表示します。

